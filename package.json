{
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "./start.sh",
    "build:server": "lerna run build --scope={@skotch/common,@skotch/server}",
    "build:web": "lerna run build --scope={@skotch/common,@skotch/client}",
    "docker:push": "docker push tomcorey26/swipe-skotch:1.0.0"
  },
  "name": "swipe-skotch",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}
